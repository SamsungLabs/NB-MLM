{
    batch_size: 8,
    update_freq: 4,
    num_classes: 2,

    valid_subset: "valid",
    
    total_num_update: 2343,
    max_update: 2343,
    max_epoch: 4,
    warmup_updates: 500,

    lr: 1e-5,
    weight_decay: 0.1,

    validate_interval_updates: 200,
    save_interval_updates: 200,
}
